mixin area(area)
    div
      h4
        a(href=`/area/${area.id}`)= area.area
        div
          p 법원명: #{area.scourt}
          div  
            ul
              each item in network         
                li= item
          div
            small Subnet : #{area.subnet}          
          
      
